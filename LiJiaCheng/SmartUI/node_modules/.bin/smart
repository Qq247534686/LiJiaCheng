#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smart/bin/smart" "$@"
  ret=$?
else 
  node  "$basedir/../smart/bin/smart" "$@"
  ret=$?
fi
exit $ret
